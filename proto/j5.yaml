---
packages:
  - label: Test
    name: test.v1

options:
  subPackages:
    - name: service
    - name: topic

dependencies:
  - registry:
      owner: pentops
      name: j5

  - registry:
      owner: pentops
      name: messaging

  - bufRegistry:
      owner: bufbuild
      name: protovalidate

  - bufRegistry:
      owner: googleapis
      name: googleapis
